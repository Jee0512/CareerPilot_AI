# CAREERPILOT UI — DISCOVERY PHASE ONLY

You are helping me rebuild the UI of an existing Streamlit application.

You are NOT allowed to redesign, refactor, install dependencies, or modify the project yet.

Your job in this phase is to **collect accurate technical facts from the actual repository** so that I can give the findings to another senior engineer/AI and make the correct architecture decisions.

Think of yourself as a codebase investigator.

---

# OBJECTIVE

I want to build a:

* White/light-only
* Premium
* Modern
* Responsive
* Accessible
* Clean
* Maintainable

Streamlit UI.

I am considering Tailwind CSS, but **do not assume Tailwind is already the correct implementation strategy**.

I need factual information from the repository first.

---

# RULE 1 — DO NOT MODIFY ANYTHING

Do NOT:

* modify files
* create files
* delete files
* install packages
* update dependencies
* change CSS
* change Streamlit configuration
* refactor Python
* redesign UI

READ ONLY.

---

# RULE 2 — INSPECT THE ACTUAL CODE

Do not rely only on:

* README
* PROJECT_CONTEXT.md
* design documents
* comments

Inspect the actual implementation.

If documentation conflicts with implementation, report the conflict.

---

# 1. STREAMLIT VERSION

Find the exact Streamlit version.

Report:

```text
Streamlit version:
Python version if discoverable:
requirements source:
Other relevant UI dependencies:
```

---

# 2. APPLICATION ENTRY

Inspect `app.py` completely.

Tell me:

* How the application starts
* How routing works
* How pages are selected
* How state is initialized
* How state is modified
* Where UI is rendered
* Where business logic is executed
* Where AI calls happen
* Where file operations happen

Give me the important functions and their responsibilities.

---

# 3. PAGE INVENTORY

Find every actual page/view.

For each:

```text
Page:
Function:
File:
How user reaches it:
State required:
Widgets:
Custom HTML:
Business logic:
AI calls:
Navigation:
```

Do not infer pages from documentation.

Find them from code.

---

# 4. STREAMLIT WIDGET INVENTORY

Search the entire repository for every Streamlit UI primitive.

Include:

```text
st.button
st.form
st.form_submit_button
st.text_input
st.text_area
st.file_uploader
st.selectbox
st.multiselect
st.radio
st.checkbox
st.slider
st.tabs
st.expander
st.columns
st.container
st.empty
st.spinner
st.progress
st.download_button
st.markdown
st.html
st.write
st.metric
```

For each occurrence report:

```text
Widget:
File:
Function:
Purpose:
State dependency:
Callback/event:
Custom styling:
```

---

# 5. SESSION STATE

Find every:

```python
st.session_state
```

Report:

| Key | Created | Type | Default | Written | Read | Purpose |
| --- | ------- | ---- | ------- | ------- | ---- | ------- |

Also explain:

* Which state controls navigation
* Which state contains user data
* Which state contains generated data
* Which state contains files
* Which state is required for each page

---

# 6. CUSTOM HTML

Find every:

```python
unsafe_allow_html=True
```

and all raw HTML generation.

For each:

```text
File:
Function:
Purpose:
HTML structure:
CSS classes:
Interactive?
Uses JavaScript?
Uses Streamlit widgets?
```

Especially identify components that are purely visual versus components that participate in application interaction.

---

# 7. CURRENT CSS

Inspect the complete `assets/style.css`.

Do not summarize vaguely.

Tell me:

### Global styling

* body/background
* typography
* colors
* spacing
* layout

### Streamlit overrides

List selectors targeting:

```text
.stButton
.stTextInput
.stTextArea
.stFileUploader
.stTabs
[data-testid=...]
```

### Component styling

List major custom classes.

### CSS variables

List all CSS variables.

### Legacy/dead CSS

Identify CSS that appears unused.

---

# 8. TAILWIND

Search the entire repository for:

```text
tailwind
cdn.tailwindcss.com
@tailwind
@apply
tw-
```

Tell me EXACTLY:

* Where Tailwind is currently loaded
* How it is loaded
* Which files use Tailwind classes
* Whether Tailwind is compiled
* Whether Tailwind is CDN-based
* Whether Tailwind configuration exists
* Whether Tailwind is actually working
* Whether Tailwind classes are used inside Streamlit-native widgets
* Whether there are CSS conflicts

Do not recommend anything yet.

Just report facts.

---

# 9. STREAMLIT CONFIGURATION

Inspect:

```text
.streamlit/config.toml
```

Report:

* theme
* primary color
* background
* secondary background
* text color
* font
* layout
* toolbar
* browser settings
* any custom configuration

---

# 10. UI COMPONENT ARCHITECTURE

Inspect:

```text
modules/ui_components.py
```

For every component/function:

```text
Name:
Purpose:
Inputs:
Outputs:
Returns HTML?
Returns Streamlit elements?
Uses CSS?
Uses SVG?
Uses JavaScript?
Used by:
```

Then determine whether this file is:

* a component library
* HTML helper
* CSS helper
* mixed UI/business code
* something else

---

# 11. BUSINESS/UI COUPLING

Find places where a UI event immediately executes:

* Gemini
* PDF processing
* parsing
* matching
* career logic
* interview logic

Example:

```python
if st.button(...):
    expensive_business_operation()
```

List these locations.

For each:

```text
File:
Function:
UI:
Business operation:
State mutation:
Risk of UI refactor:
```

---

# 12. LAYOUT ARCHITECTURE

For every page determine how layout is created.

Examples:

```text
st.columns
st.container
st.sidebar
custom HTML flex
custom HTML grid
CSS grid
CSS flex
```

Report actual usage.

---

# 13. RESPONSIVE BEHAVIOR

Inspect the CSS and HTML.

Determine:

* existing media queries
* mobile styles
* desktop styles
* fixed widths
* fixed heights
* problematic layouts
* Streamlit columns
* custom flex/grid

Do NOT recommend fixes yet.

Just report current behavior.

---

# 14. ICONS

Find every icon mechanism:

* emoji
* SVG
* image
* icon package
* Unicode
* CSS icons

Report exactly what is currently used.

---

# 15. FONTS

Find:

* font imports
* font-family declarations
* Streamlit font configuration
* Google Fonts
* local fonts

Report actual implementation.

---

# 16. DESIGN SYSTEM TOKENS

Extract all actual:

### Colors

### Typography

### Spacing

### Radius

### Shadows

### Transitions

### Breakpoints

Do not use documentation values unless they exist in the implementation.

---

# 17. CURRENT DESIGN SYSTEM CONFLICTS

Compare:

```text
Documentation
vs
CSS
vs
Streamlit config
vs
Python components
vs
actual page implementation
```

List every conflict.

Example:

```text
Conflict:
Documentation says X
Implementation uses Y
Files:
Severity:
```

---

# 18. DEPENDENCY INVENTORY

Inspect:

* requirements.txt
* pyproject.toml if present
* package files
* frontend files
* Tailwind files
* CSS tooling

Separate:

### Runtime dependencies

### UI dependencies

### Build dependencies

### Development dependencies

---

# 19. FILE STRUCTURE

Report the actual project structure.

Then identify:

### UI files

### Business files

### AI files

### Configuration

### Assets

### Legacy

---

# 20. DO NOT MAKE ARCHITECTURAL DECISIONS

This is critical.

At this stage DO NOT tell me:

> "Use Tailwind CDN."

Do NOT tell me:

> "Move everything to React."

Do NOT tell me:

> "Use custom HTML."

Do NOT tell me:

> "Rewrite app.py."

Do NOT tell me:

> "Delete the old CSS."

Those decisions come later.

---

# 21. FINAL OUTPUT

Create:

`UI_DISCOVERY.md`

Use exactly this structure:

```text
# UI DISCOVERY

## 1. Streamlit Version

## 2. Application Entry

## 3. Page Inventory

## 4. Streamlit Widget Inventory

## 5. Session State

## 6. Custom HTML

## 7. Current CSS

## 8. Current Tailwind Usage

## 9. Streamlit Configuration

## 10. UI Component Architecture

## 11. Business/UI Coupling

## 12. Layout Architecture

## 13. Responsive Behavior

## 14. Icons

## 15. Fonts

## 16. Actual Design Tokens

## 17. Design System Conflicts

## 18. Dependencies

## 19. File Structure

## 20. Unknowns
```

---

# 22. FINAL QUALITY CHECK

Before finishing:

Search the repository again specifically for:

```text
st.
unsafe_allow_html
tailwind
class=
style=
[data-testid
session_state
```

Make sure nothing important was missed.

---

# FINAL RULE

Do NOT solve the architecture.

Do NOT redesign.

Do NOT modify anything.

I want **facts first**.

The next step will be to give `UI_DISCOVERY.md` to a senior architectural reviewer who will determine the correct Streamlit + Tailwind + CSS + component architecture.

ONLY produce `UI_DISCOVERY.md`.

