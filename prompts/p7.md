PHASE 4.5 — COMPLETE LANDING PAGE ISOLATION

You have completed the Architecture Verification Audit.

The audit identified one concrete architectural boundary leak:

pages/landing.py is described as the reference implementation for the new UI foundation, but it still imports/uses legacy UI components from modules/ui_components.py:

- render_top_navbar()
- privacy_note()

Those components depend on the legacy assets/style.css system.

This must be fixed before migrating any other page.

IMPORTANT:
This is NOT a general UI refactor.
This is NOT a component-library rewrite.
This is NOT a business-logic refactor.

The objective is ONLY to make landing.py genuinely independent of the legacy UI system.

==================================================
1. READ FIRST
==================================================

Read:

1. AGENT.md
2. docs/design_system_locked.md
3. docs/ui_architecture_plan.md
4. docs/architecture/design-foundation-report.md
5. docs/architecture/current_ui_state.md
6. The Architecture Verification Report
7. pages/landing.py
8. modules/ui_components.py
9. styles/tokens.css
10. styles/base.css
11. styles/streamlit.css
12. assets/style.css
13. app.py

Treat the repository as the source of truth.

==================================================
2. OBJECTIVE
==================================================

After this phase:

pages/landing.py MUST NOT depend on:

- modules/ui_components.py
- legacy cp-* components
- assets/style.css for its visual rendering

Landing must use the NEW design foundation only:

- styles/tokens.css
- styles/base.css
- styles/streamlit.css
- native Streamlit APIs
- st.html() where static HTML presentation is actually required

The legacy CSS may remain globally loaded because other pages still depend on it.

DO NOT remove assets/style.css globally.

DO NOT migrate other pages.

==================================================
3. FIX ONLY THE ACTUAL LEAKS
==================================================

The audit identified:

A. render_top_navbar()
B. privacy_note()

Replace these usages in landing.py with new-foundation equivalents.

For the navbar:

- preserve the existing landing-page behavior
- preserve navigation destinations
- preserve buttons/actions
- preserve session-state semantics
- preserve visual intent
- use the new design tokens
- use .cp-ui namespace
- use st.html() for static presentation
- do not use legacy .cp-navbar
- do not import modules.ui_components

For privacy_note:

- preserve its meaning/content
- use the new .cp-ui styling
- use st.html()
- do not import the legacy component

==================================================
4. DO NOT CREATE A GENERIC COMPONENT SYSTEM YET
==================================================

IMPORTANT:

Do NOT create:

- a large new ui/components.py abstraction
- a generic component framework
- React
- custom Streamlit Components
- JavaScript framework
- Tailwind
- another CSS framework
- another design-token system

Do not abstract something merely because it is currently HTML.

Only extract a helper if it is clearly reusable and the abstraction has an obvious stable API.

The goal is to remove the architectural dependency, not to invent a new framework.

If the navbar/privacy implementation is only used by landing.py, keeping a small local rendering helper is acceptable.

We will establish the reusable component architecture AFTER several pages have been migrated and actual repetition is known.

==================================================
5. DESIGN SYSTEM RULES
==================================================

The locked design system is authoritative.

Use:

- existing tokens
- existing .cp-ui namespace
- existing typography scale
- existing spacing scale
- existing radii
- existing shadows
- existing semantic colors
- existing responsive rules

DO NOT introduce arbitrary values unless absolutely required by the locked specification.

DO NOT create duplicate tokens.

DO NOT create a second naming convention.

DO NOT use raw hex colors in Python HTML strings when an existing token can express the value.

==================================================
6. STREAMLIT IMPLEMENTATION RULES
==================================================

Prefer native Streamlit APIs whenever they provide the required behavior.

For static HTML presentation:

    st.html()

Use st.markdown(..., unsafe_allow_html=True) only where there is a demonstrated Streamlit limitation requiring it.

Do NOT use:

- Tailwind
- CDN CSS
- CDN JavaScript
- external icon libraries
- arbitrary JavaScript
- fragile Streamlit DOM hacks
- [data-testid] selectors for the new landing implementation

Do not recreate native Streamlit widgets using HTML.

Native buttons must remain native Streamlit buttons when they require Python interaction.

==================================================
7. ICONS
==================================================

If the landing navbar requires the CareerPilot logo/icon:

Use the existing ui/icons.py architecture.

Do not create duplicate SVG definitions.

Do not introduce an icon dependency.

==================================================
8. BUSINESS LOGIC PROTECTION
==================================================

DO NOT modify:

- modules/matcher.py
- modules/resume_parser.py
- modules/career_tools.py
- modules/gemini_client.py
- modules/career_readiness.py
- modules/interview.py
- modules/job_recommender.py
- modules/jd_input.py

Do not change:

- AI behavior
- API calls
- parsing
- matching
- scoring
- session-state semantics
- page navigation behavior

This phase is presentation-boundary cleanup only.

==================================================
9. LEGACY CSS PROTECTION
==================================================

Do NOT delete:

assets/style.css

Do NOT remove legacy CSS loading from app.py.

Other pages still depend on it.

Do not migrate legacy components globally.

Do not change modules/ui_components.py except if an absolutely necessary import cleanup is required.

The goal is:

landing.py → independent

other pages → unchanged

==================================================
10. VALIDATION
==================================================

After implementation verify:

### Static verification

Search landing.py for:

- modules.ui_components
- ui_components
- render_top_navbar
- privacy_note
- cp-navbar
- legacy component classes

There must be no legacy UI dependency remaining.

### CSS verification

Confirm landing-specific markup uses:

- .cp-ui
- new design tokens
- new foundation classes

and does not require assets/style.css.

### Functional verification

Confirm:

- navbar navigation still works
- Get Started/action buttons still work
- landing page loads
- no import errors
- no session-state regressions
- no routing regressions

### Isolation test

Perform a conceptual isolation test:

If assets/style.css were temporarily unavailable, landing.py should still have all CSS required for its own UI.

DO NOT actually delete assets/style.css.

==================================================
11. VISUAL REGRESSION
==================================================

Do not redesign the landing page.

The goal is:

OLD INTENT
      ↓
NEW IMPLEMENTATION

not:

OLD DESIGN
      ↓
NEW DESIGN

Preserve:

- hierarchy
- spacing intent
- CTA placement
- navigation behavior
- content
- responsive behavior

Only change what is necessary to move the landing implementation onto the new foundation.

==================================================
12. DOCUMENTATION
==================================================

After implementation update:

docs/architecture/design-foundation-report.md

Add a short section:

"Landing Isolation Completion"

Document:

- what legacy dependencies were removed
- what remains intentionally legacy
- why assets/style.css remains globally loaded
- why no generic component library was introduced yet

Do NOT rewrite the entire documentation.

==================================================
13. FINAL AUDIT
==================================================

Before stopping, answer:

1. Is landing.py now independent of modules/ui_components.py?
2. Does landing.py require assets/style.css?
3. Are all landing-specific custom styles inside the new foundation?
4. Are native Streamlit widgets still used for interactive behavior?
5. Did any business logic change?
6. Did any routing/state behavior change?
7. Did we introduce unnecessary abstraction?
8. Is the landing page now a valid reference implementation?

Then produce:

# PHASE 4.5 COMPLETION REPORT

Include:

- Changes made
- Files changed
- Files intentionally untouched
- Legacy dependencies removed
- Validation performed
- Remaining legacy architecture
- Risks
- Recommendation for next phase

==================================================
14. STOP CONDITION
==================================================

STOP after completing Phase 4.5.

Do NOT automatically begin migrating upload_resume.py.

Do NOT refactor modules/ui_components.py globally.

Do NOT delete assets/style.css.

Do NOT redesign anything else.

The next phase will be decided only after reviewing this completion report.

==================================================
SENIOR ENGINEERING PRINCIPLE
==================================================

Do not optimize the architecture for hypothetical future requirements.

Optimize for:

1. correctness
2. Streamlit-supported patterns
3. clear boundaries
4. minimal complexity
5. maintainability
6. measured reuse
7. incremental migration

When uncertain, inspect the current repository and current official Streamlit guidance before making an architectural decision.

For architectural questions where community practice is relevant, consult experienced developer discussions/Reddit as secondary evidence.

Never follow Reddit blindly.

Never follow an existing document blindly.

Never follow an AI-generated plan blindly.

Use evidence + repository facts + official Streamlit guidance + senior engineering judgment.

IMPLEMENT PHASE 4.5 NOW.