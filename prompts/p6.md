PLAN REVISION REQUIRED — DO NOT IMPLEMENT YET

The implementation plan is close, but do not execute it as written.

There are two issues that must be resolved before implementation.

============================================================
1. DO NOT BREAK UNMIGRATED PAGES
============================================================

Your plan says:

"Removing Tailwind CDN might temporarily break visual styling on
the un-migrated pages."

That is NOT acceptable.

We are migrating incrementally, but the application must remain
functionally and visually coherent after every phase.

Therefore:

DO NOT remove the Tailwind CDN globally yet if doing so causes
unmigrated pages to lose required styling.

Instead:

1. Inspect every current Tailwind usage.
2. Determine whether Tailwind is loaded globally or locally.
3. Determine exactly which pages/components depend on it.
4. Migrate those dependencies or establish a safe transition.
5. Only remove the CDN once no active page depends on it.

IMPORTANT:

We are not preserving Tailwind as the final architecture.

We are simply avoiding a broken intermediate state.

The final architecture remains:

Native Streamlit
+
st.html()
+
Components v2 only when justified

No Tailwind CDN.

============================================================
2. VERIFY THE ACTUAL PAGE STRUCTURE
============================================================

Your plan assumes:

pages/landing.py

but the repository inspection appears to contain page modules such
as:

upload_resume.py
job_description.py
results.py
resume_optimize.py
career_readiness.py
interview.py
mock_interview.py

Do NOT assume the target filenames.

Inspect the actual repository after the routing migration.

Determine:

- exact page files
- exact st.Page definitions
- exact navigation structure
- which page is currently the Landing page
- which module contains the current landing implementation
- whether any old page functions still exist in app.py

Use the actual repository as the source of truth.

============================================================
3. CURRENT TASK — REVISED
============================================================

Before changing any files:

A. Inspect current repository state.

B. Produce/update:

docs/architecture/current_ui_state.md

C. Specifically document:

- current routing
- current page files
- current Tailwind loading mechanism
- every active Tailwind dependency
- current CSS loading mechanism
- current Streamlit theme
- current UI component functions
- current Landing implementation
- current unsafe_allow_html usage
- current st.html usage
- current session state
- current business/UI coupling

============================================================
4. DESIGN FOUNDATION MUST BE NON-DESTRUCTIVE
============================================================

Create the new design foundation, but do not remove the old system
until the new system has been proven.

The transition should be:

CURRENT
  ↓
New foundation introduced
  ↓
Landing migrated
  ↓
Landing validated
  ↓
Remaining dependencies identified
  ↓
Legacy styles progressively removed

NOT:

CURRENT
  ↓
Delete legacy system
  ↓
Hope unmigrated pages still work

============================================================
5. STREAMLIT CONFIG
============================================================

Before modifying:

.streamlit/config.toml

inspect its current contents and determine:

- current Streamlit version
- current theme settings
- whether the routing migration changed anything
- whether the application already depends on any theme values

Then propose the minimum safe changes required for the new
white/light design.

Do not overwrite unrelated configuration.

============================================================
6. DESIGN FOUNDATION
============================================================

The new foundation should establish:

styles/
    tokens.css
    base.css
    streamlit.css

ONLY if this structure is actually useful after repository inspection.

Do not create files merely because the architecture document
contains those names.

The design system must have:

- semantic colors
- typography
- spacing
- radius
- shadows
- layout primitives
- responsive rules
- accessibility basics

============================================================
7. LANDING PAGE
============================================================

The Landing page is the reference implementation.

But before rebuilding it:

1. Read the existing Landing implementation completely.
2. Identify every widget.
3. Identify every state dependency.
4. Identify every business-logic call.
5. Identify every navigation action.
6. Identify every HTML/CSS dependency.
7. Preserve the behavior.

Then rebuild only its PRESENTATION layer.

Do not alter:

- resume parsing
- state semantics
- navigation semantics
- upload behavior
- business logic

============================================================
8. CUSTOM UI TECHNOLOGY
============================================================

For every Landing component decide:

NATIVE_STREAMLIT
or
ST_HTML

Do not introduce Components v2 unless actual requirements prove
that Native Streamlit + st.html() cannot provide the required
interaction.

Do not introduce React.

Do not introduce Tailwind.

Do not create fake HTML widgets.

============================================================
9. TAILWIND TRANSITION
============================================================

The final architecture is still:

NO TAILWIND CDN.

However, removal must be staged safely.

Create a migration inventory:

| File | Tailwind dependency | Used for | Replacement | Migrated? |
|------|---------------------|----------|-------------|-----------|

Do not delete Tailwind until the inventory demonstrates that
removal will not break an active page.

============================================================
10. VALIDATION REQUIREMENT
============================================================

The application must remain runnable after this phase.

At minimum verify:

- app starts
- Landing renders
- navigation works
- all existing pages still open
- no import errors
- no missing CSS causing broken layouts
- no Tailwind-dependent page is accidentally destroyed
- session state remains intact
- business logic is unchanged

The goal is:

NEW LANDING
+
OLD PAGES STILL WORKING

not:

NEW LANDING
+
BROKEN OLD PAGES

============================================================
11. FILE PROTECTION
============================================================

Continue protecting:

modules/matcher.py
modules/resume_parser.py
modules/career_tools.py
modules/gemini_client.py
modules/career_readiness.py
modules/interview.py
modules/job_recommender.py
modules/jd_input.py

Also do not modify unrelated page modules during this phase.

============================================================
12. REQUIRED IMPLEMENTATION PLAN REVISION
============================================================

Before implementation, update implementation_plan.md to reflect:

1. Repository-first page verification.
2. Non-destructive CSS migration.
3. Tailwind dependency inventory.
4. Landing-only UI migration.
5. No deliberate breakage of unmigrated pages.
6. Validation of all existing routes after Landing migration.

Then show the revised plan.

DO NOT IMPLEMENT YET.

============================================================
13. AFTER THE REVISED PLAN
============================================================

Once the revised plan is produced, STOP.

I will review the plan before implementation.

============================================================
IMPORTANT
============================================================

The architecture decision is already made.

You do not need to debate:

- React vs Streamlit
- Tailwind vs CSS
- Components v2 vs native

at this stage.

The decision is:

Streamlit-first
+
native widgets
+
st.html() for static presentation
+
Components v2 only when a real interactive requirement
justifies it
+
React/Tailwind only inside such a component if justified.

Your current job is to execute this safely against the REAL repository.