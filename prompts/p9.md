PHASE 6 — MIGRATE JOB DESCRIPTION PAGE

Proceed with Phase 6.

OBJECTIVE
Migrate pages/job_description.py from the legacy UI system to the new .cp-ui design foundation while preserving ALL business logic and Streamlit-native interaction.

ARCHITECTURAL RULES

1. Read these first:
   - docs/agent.md
   - docs/design_system_locked.md
   - docs/ui_architecture_plan.md
   - docs/architecture/design-foundation-report.md
   - docs/architecture/current_ui_state.md
   - docs/architecture/ui_input_inventory.md
   - styles/tokens.css
   - styles/base.css
   - pages/landing.py
   - pages/upload_resume.py
   - ui/components.py

2. Treat landing.py and upload_resume.py as the current reference implementations.

3. Do NOT modify business logic.

4. Do NOT modify:
   - matcher.py
   - jd_input.py
   - gemini_client.py
   - resume_parser.py
   - other business modules

5. Remove job_description.py's dependency on:
   - modules/ui_components.py
   - legacy cp-* presentation components

6. Do NOT remove:
   - assets/style.css
   - modules/ui_components.py

They are still required by unmigrated pages.

7. Continue using native Streamlit widgets:
   - st.text_area
   - st.file_uploader
   - st.text_input
   - st.radio/tabs if already used
   - st.button
   - st.container
   - st.columns

8. Do NOT replace native inputs with custom HTML.

9. Use st.html() only for static presentation components.

10. Use existing design tokens from styles/tokens.css.
Do not invent a second token system.

11. Reuse:
   - render_step_track()
   - render_page_header()
   - render_badge_list()

from ui/components.py wherever applicable.

12. Only create another reusable component if the pattern is genuinely reusable.
Do not prematurely abstract.

JOB DESCRIPTION PAGE STRUCTURE

Target visual hierarchy:

Step tracker
↓
Page heading + supporting description
↓
Input method selection
   Paste / Upload / URL
↓
Primary input surface
↓
Validation / status feedback
↓
Company/context information where applicable
↓
Primary Continue action
↓
Secondary navigation

Keep the page visually consistent with:

landing.py
upload_resume.py

Do not make it visually identical; preserve the page's purpose and information hierarchy.

INPUTS

Audit every input currently present in job_description.py.

For each one determine:

- native Streamlit widget
- label
- helper text
- placeholder
- validation behavior
- disabled state
- loading state
- error state
- success state

Do not add visual states that the application does not actually need.

TABS / INPUT METHODS

If the page currently supports:

- Paste JD
- Upload JD
- URL

preserve all existing functionality exactly.

Do not change how JD extraction works.

Do not change URL scraping.

Do not change file parsing.

Do not change session-state keys.

Do not change navigation behavior.

UI should only change presentation.

CONTAINERS

Use native Streamlit containers for grouping interactive inputs.

Do NOT assume st.html() wraps later Streamlit widgets in the DOM.

Use st.container(border=True) where it provides meaningful grouping.

Do not turn every element into a card.

VISUAL QUALITY

Aim for:

- premium SaaS
- Apple-inspired clarity
- restrained surfaces
- generous whitespace
- strong typography hierarchy
- obvious primary CTA
- clear input hierarchy
- minimal visual noise

Avoid:

- excessive gradients
- excessive cards
- unnecessary animations
- decorative elements that compete with the JD input
- fragile CSS
- JavaScript
- fake HTML controls

SIDEBAR

Do nothing unless an actual issue is discovered.

Continue using native st.navigation().

Do not create a custom sidebar.

VALIDATION

Before completion verify:

[ ] No modules.ui_components import remains in job_description.py
[ ] No legacy cp-* presentation classes are required by job_description.py
[ ] All business logic is unchanged
[ ] All session-state behavior is unchanged
[ ] Paste JD works
[ ] Upload JD works
[ ] URL JD works
[ ] Existing validation works
[ ] Continue navigation works
[ ] Back navigation works
[ ] Step tracker works
[ ] Mobile layout remains usable
[ ] No fragile Streamlit DOM selectors were introduced
[ ] No new dependency was added unnecessarily
[ ] Existing pages still load

ARCHITECTURAL VERIFICATION

After implementation, inspect the diff.

Specifically verify that the migration changed PRESENTATION, not APPLICATION BEHAVIOR.

Then produce:

docs/architecture/phase-6-completion-report.md

Include:

1. Files changed
2. Files intentionally untouched
3. Legacy dependencies removed
4. Native Streamlit widgets retained
5. Components reused
6. New components created, if any
7. Business logic verification
8. Session-state verification
9. Responsive verification
10. Accessibility verification
11. Remaining legacy architecture
12. Technical debt
13. Risks
14. Recommendation for Phase 7

STOP if business logic needs modification.

STOP if the design requires fragile Streamlit DOM selectors.

STOP if a new abstraction has no demonstrated reuse.

Follow official Streamlit best practices first, experienced senior-engineer practice second, and community/Reddit patterns only when they do not conflict with the framework's supported architecture.

IMPLEMENT PHASE 6.