# CAREERPILOT — STREAMLIT ARCHITECTURE RESEARCH

You are NOT implementing anything yet.

You are acting as a senior Streamlit architect researching how this application should be built using **current Streamlit best practices**.

The existing project is a functional Streamlit application, but its UI architecture has grown organically.

I want to scale it properly.

Do NOT assume that the existing architecture, Tailwind CDN approach, custom router, or `unsafe_allow_html` approach is correct.

Your job is to investigate the current repository and determine the correct architecture.

---

# PART 1 — VERIFY STREAMLIT

Determine:

* Exact installed Streamlit version
* Python version
* requirements
* package manager
* deployment environment
* whether Node/npm is available
* whether a frontend build step is currently possible
* whether the application is intended to deploy on Streamlit Community Cloud, another platform, or unknown

---

# PART 2 — CURRENT STREAMLIT ARCHITECTURE

Inspect the entire application.

Document:

* entrypoint
* routing
* pages
* state
* widgets
* forms
* caching
* custom HTML
* CSS
* components
* external services
* business logic boundaries

Do not trust existing documentation if implementation differs.

---

# PART 3 — MULTIPAGE ARCHITECTURE

The current application uses a custom `st.session_state["page"]` router.

Research and evaluate the current Streamlit approaches:

1. `pages/` directory
2. `st.Page`
3. `st.navigation`
4. callable pages
5. custom navigation using `st.page_link`
6. `st.switch_page`

Determine which approach is best for this application.

Specifically answer:

* Should the current custom router remain?
* Should we migrate to `st.navigation`?
* Should pages become separate files?
* Which shared elements belong in the entrypoint?
* How should session state be shared?
* How should programmatic navigation work?
* How do we preserve the existing user journey?

Do not implement yet.

---

# PART 4 — STREAMLIT STATE ARCHITECTURE

Audit all `st.session_state`.

Determine:

* global state
* page-specific state
* transient state
* derived state
* file state
* AI result state
* navigation state

Recommend how state should be organized for a larger application.

Do NOT introduce a database just for UI architecture.

---

# PART 5 — STREAMLIT CACHING

Search for:

```text
st.cache_data
st.cache_resource
```

and determine where caching would actually be beneficial.

Pay special attention to:

* SentenceTransformer loading
* Gemini client
* PDF parsing
* expensive calculations
* external requests

Do not add caching blindly.

For every recommendation explain:

```text
What:
Why:
Cache type:
Risk:
Invalidation:
```

---

# PART 6 — UI ARCHITECTURE

Classify every current UI element as:

### Native Streamlit

### Static custom presentation

### Interactive custom component

### Business logic accidentally embedded in UI

Determine what should remain native and what should become a reusable component.

---

# PART 7 — CUSTOM COMPONENTS

Research the CURRENT Streamlit Custom Components architecture.

Evaluate:

### Components v1

versus

### Components v2

Specifically investigate:

* iframe isolation
* HTML
* CSS
* JavaScript
* bidirectional communication
* state
* events
* theming
* package-based components
* TypeScript
* Vite
* React

Determine whether CareerPilot actually needs Components v2.

Do not introduce components merely because they are newer.

Identify which CareerPilot UI elements genuinely justify them.

---

# PART 8 — TAILWIND

The project currently uses:

```text
Tailwind CDN
+
unsafe_allow_html
+
native Streamlit CSS overrides
```

This is NOT automatically accepted.

Evaluate:

### A. No Tailwind — pure CSS/design tokens

### B. Compiled Tailwind

### C. Tailwind only for custom HTML

### D. Tailwind inside Components v2

### E. Tailwind + TypeScript

### F. Tailwind + React

### G. Hybrid

For every option evaluate:

* Streamlit compatibility
* maintainability
* build complexity
* deployment
* performance
* CSS isolation
* responsive behavior
* AI-agent friendliness
* debugging
* upgrade safety
* bundle size
* developer experience

Then recommend ONE.

---

# PART 9 — CSS ARCHITECTURE

Determine how a large Streamlit application should handle CSS.

Research:

* Streamlit theme configuration
* CSS custom properties
* native widget styling
* custom component CSS
* global CSS
* scoped CSS
* DOM selectors
* `data-testid`
* CSS specificity
* `unsafe_allow_html`

Determine:

> What CSS should be global?

> What CSS should be component-scoped?

> What CSS should NEVER target Streamlit internals?

> When is DOM overriding justified?

---

# PART 10 — THEME

The product must be:

# LIGHT / WHITE ONLY

Determine how much of the visual system can be handled through:

```text
.streamlit/config.toml
```

versus:

```text
design tokens
```

versus:

```text
component CSS
```

Create a hierarchy of sources of truth.

---

# PART 11 — DESIGN SYSTEM

Research how to structure a scalable design system in a Streamlit application.

Recommend:

```text
Tokens
 ↓
Primitives
 ↓
Components
 ↓
Product components
 ↓
Pages
```

Determine where each layer should live.

---

# PART 12 — ICONS

Evaluate:

* Material icons available through Streamlit
* SVG
* Lucide
* icon packages
* emoji

Choose a maintainable approach.

---

# PART 13 — TYPOGRAPHY

Determine the best way to implement:

* font family
* weights
* sizes
* line heights
* hierarchy

without creating fragile external dependencies.

---

# PART 14 — RESPONSIVE DESIGN

Research Streamlit's current responsive behavior.

Determine the correct combination of:

* `st.columns`
* containers
* CSS
* custom components
* responsive utilities

Do NOT assume Tailwind solves Streamlit's layout system.

---

# PART 15 — AI CODING WORKFLOW

Research how Streamlit recommends using AI coding agents for:

* Streamlit apps
* custom components
* TypeScript
* frontend work
* testing

Determine how we should structure instructions for an AI coding agent.

Specifically recommend:

* context files
* skills
* task boundaries
* validation
* incremental changes
* testing
* code review

---

# PART 16 — PROJECT STRUCTURE

Based on all research, propose the ideal scalable structure for CareerPilot.

Do NOT blindly use this:

```text
pages/
components/
services/
styles/
```

Determine the structure from Streamlit's actual architecture.

---

# PART 17 — DO NOT IMPLEMENT

This entire task is research and architecture only.

Do NOT:

* modify files
* install dependencies
* migrate routing
* install Tailwind
* create components
* change CSS
* redesign pages

---

# FINAL OUTPUT

Create:

`STREAMLIT_ARCHITECTURE_RESEARCH.md`

Structure:

# 1. Current Application

# 2. Current Streamlit Architecture

# 3. Current Problems

# 4. Current Streamlit Best Practices Relevant to CareerPilot

# 5. Multipage Recommendation

# 6. State Recommendation

# 7. Caching Recommendation

# 8. Native Widget Strategy

# 9. Custom Component Strategy

# 10. Components v1 vs v2

# 11. Tailwind Options

# 12. CSS Architecture

# 13. Theme Architecture

# 14. Design System Architecture

# 15. Responsive Strategy

# 16. Icon Strategy

# 17. Typography Strategy

# 18. AI Coding Workflow

# 19. Recommended Project Structure

# 20. Recommended Technology Stack

# 21. Migration Strategy

# 22. Risks

# 23. Things We Should NOT Do

# 24. Open Questions

# FINAL DECISION

End with:

```text
Streamlit architecture:
Multipage architecture:
State architecture:
Caching:
Native widgets:
Custom components:
Components version:
Tailwind:
Tailwind integration:
CSS:
Design tokens:
Icons:
Typography:
Frontend build tooling:
AI coding workflow:
Recommended project structure:
```

For every recommendation provide the reasoning.

Do not implement anything.

This document will be reviewed before implementation begins.
